package ch.brunostuessy.algo.strategy;

/**
 * The position taking signal.
 * 
 * @author Bruno Stüssi
 *
 */
public enum PositionSignal {

	INVALID(), NONE(), OPENLONG(), CLOSELONG(), OPENSHORT(), CLOSESHORT()

}
